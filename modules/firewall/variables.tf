# Used as a prefix for all firewall resource names and the CW dashboard name.
variable "project_name" {}

# AWS region — passed to the CloudWatch dashboard widgets for metric scoping.
variable "region" {}
